import { StripeFormDirective } from './stripe-form.directive';

describe('StripeFormDirective', () => {
  it('should create an instance', () => {
    const directive = new StripeFormDirective();
    expect(directive).toBeTruthy();
  });
});
